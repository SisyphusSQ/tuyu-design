# 图屿 Studio 设计原型协作规则

## 设计来源强关联

本目录是图屿 Studio 设计可视化 Web 原型，内容来源强关联：

`/Users/suqing/Coding/golang/00_self/tuyu-studio/docs/design`

维护本目录时，必须把上述设计目录视为原型内容的主要来源真相。本目录不应反向修改源设计文档，也不应把生成的 App 文件写入源仓库。

## 设计变动同步规则

如果 `/Users/suqing/Coding/golang/00_self/tuyu-studio/docs/design` 中的设计文档发生变化，维护本原型前必须先对比变动内容，并判断是否影响当前原型中的设计摘要、页面结构、模块卡片、流程说明、错误/恢复语义或 UI 草图。

不允许因为源设计目录有变化就自动全量同步更新本原型。同步前必须向用户二次确认：

- 哪些设计变动需要同步到本原型。
- 哪些变动暂不进入本原型。
- 是否需要调整生产流程页、设计模块页、数据对象或视觉草图。
- 是否仍保持“只读源设计文档、不修改源仓库”的边界。

未经二次确认时，只能进行只读比对、影响分析和同步建议，不直接修改原型内容。
